# gene
基因组装代码

k-mer作用：
hifiasm 会先把父亲和母亲的数据全部切成 31-mer。
它会找出只有父亲有、母亲绝对没有的 K-mer（父本特异性 K-mer），以及只有母亲有、父亲绝对没有的 K-mer（母本特异性 K-mer）。
接下来，它拿起一条子代的 HiFi read，看看这条 read 上包含哪种 K-mer。如果包含大量父本特异性 K-mer，hifiasm 闭着眼睛就能把它扔进“父亲单倍型”的阵营里。
这种定相极其精准，彻底解决了组装图里的气泡死结。
